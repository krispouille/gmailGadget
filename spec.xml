<?xml version="1.0" encoding="UTF-8" ?> 
<Module>
    <ModulePrefs title="UseIt" description="just do it" height="200" author="krispouille" author_email="..." author_location="France">
        <Require feature="dynamic-height" />
        <Require feature="google.contentmatch">
            <Param name="extractors">google.com:SubjectExtractor</Param>
        </Require>
    </ModulePrefs>

    <Content type="html" view="card"><![CDATA[ 
        <script type='text/javascript' src='http://www.google.com/jsapi'></script>
        <script type="text/javascript">
             function require(url, condition, callback){
                  condition = condition || false;
                  if(condition){
                      var done = false;
                      var script = document.createElement("script");
                      script.src = url;
                      script.onload = script.onreadystatechange = function(){
                          if (!done && (!this.readyState || this.readyState == "loaded" || this.readyState == "complete")) {
                              done = true;
                              callback();
                          }
                      };
                      document.body.appendChild(script);
                  }
                  else{
                      callback();
                  }
              };
              require('http://ajax.googleapis.com/ajax/libs/jquery/1.10.1/jquery.min.js', true, function(){
                alert(jQuery.fn.jquery);
              });
        </script>
    ]]></Content>
</Module>
